**Follow the steps for running the project**
* Clone the repo from git
* Create virtualenv  
* Install the Requirements 
  - pip install -r requiremets.txt
* Move the project folder
  - cd backend
* Run the server
  - python manage.py runserver
    